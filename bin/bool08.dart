/*
    Create function func
    check the whole number. Integers are 0 and a positive number.
    Args:
        a: int
    Returns:
        bool
    */

void main() {
  bool func(int a) {
    return ((a) == int);
    return a >= 0;
  }

  void main() {
    int son = 5;
    print(son);

    print(func(5));
  }
}
